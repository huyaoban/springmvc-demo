<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
       		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd
       		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd">

	<!-- 自动扫描基于注解的控制器，包括@Controller, @Service -->
	<context:component-scan base-package="com.huyaoban.springmvc.controller" />
	<context:component-scan base-package="com.huyaoban.springmvc.service" />
	
	<!-- 注册HandlerMapper、HandlerAdapter两个映射类 -->
	<mvc:annotation-driven conversion-service="conversionService" />
	
	<!-- 因为配置的spring是拦截所有请求，所以需要配置<mvc:default-servlet-handler />，来让静态资源通过（如js, css文件等） -->
	<!-- <mvc:default-servlet-handler /> -->
	
	<!-- 静态资源,需要单独处理.不经过DispatcherServlet -->
	<mvc:resources mapping="/css/**" location="/css/" />
	<mvc:resources mapping="/*.html" location="/" />
    
    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">  
        <property name="prefix" value="/WEB-INF/jsp/" />  
        <property name="suffix" value=".jsp" />  
    </bean>
    
    <!-- 要使用formatter，需要添加component-scan元素 -->
	<context:component-scan base-package="com.huyaoban.springmvc.formatter" />
	<!-- 要使用formatter，需要注册这个bean，并传给annotation-driven。该bean也可以使用formatters和converters属性来传递转换器和格式化器 -->
    <bean id="conversionService" class="org.springframework.format.support.FormattingConversionServiceFactoryBean">
    	<property name="formatters">
    		<list>
    			<bean class="com.huyaoban.springmvc.formatter.String2DateFormatter">
    				<constructor-arg type="java.lang.String" value="yyyy-MM-dd"/>
    			</bean>
    		</list>
    	</property>
    </bean>

</beans>